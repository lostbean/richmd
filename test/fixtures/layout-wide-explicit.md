---
richmd-layout: wide
---

# A wide-layout document (explicit)

This document explicitly sets `richmd-layout: wide` frontmatter, which is
also the default when the key is absent entirely (design.md §07).
